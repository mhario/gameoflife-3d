const assert = require('chai').assert;
const expect = require('chai').expect;

import { createCell, buildBoard } from '../app/components/Board';


describe('Board building functions', function() {

  describe('createCell function', function() {

    it('should be a three.js mesh', function() {
      // assert.equal(-1, [1,2,3].indexOf(4))
    })

    // these tests aint real..
    // YOU aint real..

    it('should feature puppies', function() {
      assert.equal(2, 1+1)
    })
  })

});
